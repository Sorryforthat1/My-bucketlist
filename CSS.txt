body, h1, p, ul, li {
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Arial', sans-serif;
  background-color: #f5f5f5;
  color: #333;
  text-align: center;
}

header {
  background-color: #4CAF50;
  color: white;
  padding: 20px;
}

h1 {
  font-size: 2.5rem;
}

ul {
  list-style-type: none;
  padding: 20px;
}

li {
  font-size: 1.2rem;
  margin: 10px 0;
  padding: 10px;
  background-color: #ffffff;
  border-radius: 5px;
  display: flex;
  align-items: center;
}

li i {
  margin-right: 10px;
}

.icon-check {
  color: green;
}

.icon-star {
  color: gold;
}

footer {
  margin-top: 20px;
  font-size: 0.9rem;
  color: #777;
}

/* Media Query for Mobile Responsiveness */
@media (max-width: 600px) {
  li {
    font-size: 1rem;
    padding: 8px;
  }
}
/* List Items Style */
li {
  font-size: 1.2rem;
  margin: 10px 0;
  padding: 10px;
  background-color: #ffffff;
  border-radius: 5px;
  display: flex;
  align-items: center;
  transition: transform 0.3s ease, background-color 0.3s ease;
}

/* Hover Effect */
li:hover {
  background-color: #e0f7fa; /* Light cyan background on hover */
  transform: scale(1.05); /* Slight zoom-in effect */
}

/* Icon Color Change on Hover */
li:hover i {
  color: #004d40; /* Darker green for the icon on hover */
}

/* Adding Smooth Transitions to the Text */
li:hover {
  color: #004d40; /* Text color change on hover */
}

/* Icon Styles */
.icon-check {
  color: green;
}

.icon-star {
  color: gold;
}
/* Header Styling */
header {
  background-color: #0288d1; /* Custom blue background */
  color: white;
  padding: 40px 20px;
  text-align: center;
  border-bottom: 5px solid #01579b; /* Darker blue border at the bottom */
}

header h1 {
  font-size: 3rem;
  font-family: 'Arial', sans-serif;
  letter-spacing: 2px;
  margin-bottom: 10px;
}

header p {
  font-size: 1.2rem;
  font-style: italic;
  letter-spacing: 1px;
}

/* Footer Styling */
footer {
  background-color: #263238; /* Dark grey background for the footer */
  color: white;
  padding: 20px 10px;
  text-align: center;
  font-size: 1rem;
  border-top: 5px solid #455a64; /* Light grey border at the top */
}

footer p {
  margin: 0;
}

footer a {
  color: #80deea; /* Light cyan link color */
  text-decoration: none;
  font-weight: bold;
}

footer a:hover {
  text-decoration: underline;
}
